Horizontal XP bar toward the next level, with an optional "Lv N" chip and value/goal count.

```jsx
<XPBar value={240} max={500} level={7} />
<XPBar value={80} max={100} tone="brand" unit="pts" />
```

Props: `value`, `max`, `level`, `tone` (accent/brand/success), `showCount`, `unit`.
